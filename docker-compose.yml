version: '3.8'

services:
  frontend-admin:
    build:
      context: ./frontend-admin
      dockerfile: Dockerfile
    container_name: gx-culture-heritage-frontend
    ports:
      - "8081:80"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
